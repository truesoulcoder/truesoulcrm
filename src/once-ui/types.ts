// Shared types for once-ui components

// Color schemes and weights for theming
export type ColorScheme = string;
export type ColorWeight = string;

// Spacing token, e.g., "small", "medium", etc.
export type SpacingToken = string;

// Text variant, e.g., "body-default-m"
export type TextVariant = string;
